This distribution system is laid out at the repository root (same level as this folder):

  backend/      — FastAPI + PostgreSQL API
  mobile-app/   — Expo React Native + SQLite (entry via ../index.js)
  admin-panel/  — Next.js admin dashboard

Run backend from backend/, mobile from repo root (npm start), admin from admin-panel/ (npm run dev).
